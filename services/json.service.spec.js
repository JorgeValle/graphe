describe('JSON service', function() {

  const jsonService = require('./json.service'),
        sendResponse = jsonService.sendResponse;

  // it('should have method to build and send response', function() {
  //   expect(sendResponse('Test', 200, 'Test content')).toBeDefined();
  // });
});